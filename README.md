# Order Management Service — Starter Project

Simple Spring Boot project (teaching scaffold) demonstrating:
- REST API with Spring Web
- Service layer, Repository (JPA) and Entity
- H2 in-memory DB for local dev
- Unit tests with MockMvc and Mockito
- CI using GitHub Actions

How to run locally:
```bash
mvn spring-boot:run
```

Swagger / Postman: use `POST /api/orders` with JSON {"customer":"Alice","amount":12.0}.

Files generated from assistant for learning purposes. Original CV file: /mnt/data/CV_Yaroslav_Mykhalkiv.pdf
